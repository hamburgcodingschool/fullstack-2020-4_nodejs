const numbers = [10, 4, 2, 10, 5];

// 1 - print all the numbers in the array one by one

// 2 - print all the numbers in the array one by one multiplied by 2

for (let i = 0; i < numbers.length; i++) {
    // if (i % 3 == 0) {
    //     console.log();
    // }
    console.log(`${i}: ${numbers[i]} x 2 = ${numbers[i] * 2}`);
}